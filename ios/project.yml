name: Hermoso
options:
  bundleIdPrefix: com.hermoso
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

configs:
  Debug: debug
  Release: release

packages:
  Kingfisher:
    url: https://github.com/onevcat/Kingfisher.git
    from: 7.11.0
  KeychainAccess:
    url: https://github.com/kishikawakatsumi/KeychainAccess.git
    from: 4.2.2

targets:
  Hermoso:
    type: application
    platform: iOS
    sources:
      - path: Hermoso
    info:
      path: Hermoso/Info.plist
      properties:
        CFBundleDisplayName: Hermoso
        UILaunchScreen:
          UIColorName: LaunchBackgroundColor
        NSCameraUsageDescription: "Hermoso needs camera access to perform your AI skin scan."
        NSLocationWhenInUseUsageDescription: "Hermoso uses your location to show nearby salons and estimate distance."
        UIAppFonts:
          - CormorantGaramond-Light.ttf
    dependencies:
      - package: Kingfisher
      - package: KeychainAccess
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hermoso.ios
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

schemes:
  Hermoso:
    build:
      targets:
        Hermoso: all
    run:
      config: Debug
    test:
      config: Debug
